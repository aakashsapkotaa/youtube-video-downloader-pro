services:
  - type: web
    name: youtube-downloader-api
    env: node
    plan: free
    buildCommand: npm install && chmod +x bin/yt-dlp
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        sync: false
